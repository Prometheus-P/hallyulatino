---
const feature = Astro.props.feature || {};
---

<div class="card">
  <h3>Previsualización</h3>
  <p class="muted">Vista previa rápida con estilos del sitio.</p>
  <div class="hero" data-preview="hero">
    <div class="hero-text">
      <p class="eyebrow" data-preview="category">{feature.category ?? 'Categoría'}</p>
      <h1 data-preview="title">{feature.title ?? 'Título de la nota'}</h1>
      <p data-preview="description">{feature.descriptionEs ?? 'Descripción en español para la audiencia latina.'}</p>
      <p class="hooks" data-preview="latamHook">Incluye ganchos para fans LatAm.</p>
    </div>
    <div class="hero-img" data-preview="heroImage">
      <span>Hero</span>
    </div>
  </div>
  <section class="body">
    <h4>Contenido</h4>
    <div data-preview="blocks">
      <p>Agrega bloques para verlos aquí.</p>
    </div>
  </section>
  <section class="meta">
    <div>
      <strong>Países</strong>
      <p data-preview="countries">MX, CL</p>
    </div>
    <div>
      <strong>Tags</strong>
      <p data-preview="tags">kpop, conciertos</p>
    </div>
    <div>
      <strong>Fecha</strong>
      <p data-preview="publishDate">2024-12-03</p>
    </div>
  </section>
</div>

<style>
  .card {
    background: #121a30;
    border: 1px solid #1f2a44;
    border-radius: 12px;
    padding: 16px;
  }
  .muted {
    color: #9ba3b5;
    margin-top: 0;
  }
  .hero {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 12px;
    align-items: center;
  }
  .hero-text h1 {
    margin: 4px 0;
  }
  .hero-text p {
    margin: 2px 0;
  }
  .eyebrow {
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #9b87f5;
    font-weight: 700;
    font-size: 12px;
  }
  .hooks {
    color: #5dd39e;
    font-weight: 700;
  }
  .hero-img {
    background: #0c1427;
    border: 1px dashed #1f2a44;
    border-radius: 12px;
    min-height: 140px;
    display: grid;
    place-items: center;
    color: #9ba3b5;
  }
  .body {
    margin-top: 16px;
    border-top: 1px solid #1f2a44;
    padding-top: 12px;
  }
  .body h4 {
    margin: 0 0 8px 0;
  }
  .body p {
    margin: 8px 0;
  }
  .meta {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 12px;
    margin-top: 12px;
    border-top: 1px solid #1f2a44;
    padding-top: 12px;
  }
</style>
