<div class="card">
  <h3>Compositor de contenido (LatAm)</h3>
  <p class="muted">Campos obligatorios marcados con *</p>
  <form id="feature-form">
    <div class="grid">
      <label> Título* <input id="title" name="title" required /> </label>
      <label> Descripción (ES)* <textarea id="descriptionEs" name="descriptionEs" required></textarea> </label>
      <label>
        Categoría*
        <select id="category" name="category">
          <option value="music">Música</option>
          <option value="series">Series</option>
          <option value="event">Eventos</option>
          <option value="gastronomy">Gastronomía</option>
          <option value="culture">Cultura</option>
        </select>
      </label>
      <label> Países (coma) <input id="countries" name="countries" placeholder="México, Chile" /> </label>
      <label> LatAm hooks (coma)* <input id="latamHook" name="latamHook" placeholder="Experiencia fan, Gastronomía" required /> </label>
      <label> Hero image <input id="heroImage" name="heroImage" placeholder="/images/features/hero.jpg" /> </label>
      <label> Hero alt <input id="heroImageAlt" name="heroImageAlt" placeholder="Descripción de la imagen" /> </label>
      <label> Fecha publicación* <input id="publishDate" name="publishDate" type="date" required /> </label>
      <label> Autor <input id="author" name="author" value="OndaCoreana" /> </label>
      <label> Tags (coma) <input id="tags" name="tags" placeholder="kpop, conciertos" /> </label>
    </div>
    <label class="block">
      Bloques (uno por línea, usa "Título::Contenido")
      <textarea id="blocks" name="blocks" rows="3" placeholder="Lineup::Las bandas principales..."></textarea>
    </label>
    <label class="block">
      SEO meta (opcional) - título y descripción
      <input id="seoTitle" name="seoTitle" placeholder="Título SEO" />
      <input id="seoDescription" name="seoDescription" placeholder="Descripción SEO" />
    </label>
    <div class="actions">
      <button type="button" id="ai-generate">Generar con AI</button>
      <button type="button" id="export-mdx">Exportar MDX</button>
    </div>
  </form>
</div>

<style>
  .card {
    background: #121a30;
    border: 1px solid #1f2a44;
    border-radius: 12px;
    padding: 16px;
  }
  .muted {
    color: #9ba3b5;
    margin-top: 0;
  }
  form {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 12px;
  }
  label {
    display: flex;
    flex-direction: column;
    color: #c7d0e5;
    font-size: 14px;
  }
  input,
  select,
  textarea {
    margin-top: 6px;
    padding: 10px 12px;
    border-radius: 8px;
    border: 1px solid #1f2a44;
    background: #0c1427;
    color: #e8ecf5;
  }
  textarea {
    min-height: 96px;
  }
  .block {
    width: 100%;
  }
  .actions {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
  }
  button {
    background: linear-gradient(120deg, #9b87f5, #5dd39e);
    color: #0b1021;
    border: none;
    padding: 10px 14px;
    border-radius: 10px;
    font-weight: 700;
    cursor: pointer;
  }
</style>
